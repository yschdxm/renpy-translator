# Changelog

本项目的所有重要变更都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [v0.4.0] - 2026-09-20

### 新增

- 剧情分支图 / 人物关系图谱（实验功能，未经完整验证，不能确保准确性）：
  静态解析脚本还原剧情分支（菜单选项、call 回流、四类动态跳变静态恢复、
  Room 地点系统、沙盒事件池展开），AI 生成场景摘要与人物关系；场景卡片带
  缩略图与翻译进度着色，支持结局跳转、搜索自动展开、按文件过滤、增量更新、
  多帧缩略图手选；关系图谱支持阵营筛选、共现边、人工关系增删改、角色卡（4ae45d7、a5bee14）
- 引擎渲染（实验性中的实验性功能，不能保证正常运行，开关控制）：场景缩略图
  与角色立绘改用游戏自己的 Ren'Py 引擎在沙盒中回放渲染——真 ATL/transform、
  真 layeredimage 分层立绘、真 CG 渲染路径；选引擎渲染则失败明确报错不静默
  回退；大图分批渲染；全游戏矩阵验证（73f3bf9、e9caa28、0552549）
- 动态文本翻译体系：动态文本 f-string 模板化、!t 值通道、_ren.py 定向提取（802434a）
- 模型配置新增思考模式三态开关（跟随模型默认/开启/关闭）与温度可空
  （留空不发参数、跟随模型默认）（2aee366）
- 应用内「检查更新」（GitHub Release）：手动检查有反馈，可开启启动时自动
  检查（有新版本才弹窗，失败/无更新静默）（2aee366）
- Windows 安装包重复运行时检测已安装版本：可选原路径更新（跳过目录选择、
  保留现有数据目录）或全新安装另选路径；覆盖前自动关闭运行中的实例（2aee366）

### 优化

- 内嵌文本判定级联重构：自适应双票粗筛、精审双跑决胜、灰区锚定复核、
  判定粘性与审计链、strings 表应用（74a5dae、e0fe633）
- 内嵌扫描器与静态分析器补全：前缀字面量、新覆盖形态、多跳数据流（6493fcc）
- 译文标记硬校验全链路（markup_check 三处接入）与批失败韧性：重试只发失败
  条目、失败条目被拒译文透传与行内采用、译文换行合法性检查（d75d986、0ffeaa7）
- 翻译 token 缓存命中率优化：tools schema 常量化、术语表追加式增长、
  命中可观测（7cb6f0d）
- 人名表重构：工厂/_ren.py 角色提取、无显示名角色留空全链路、说话人归一
  根变量、台词统计口径修正（dd4d85f、69f6c1f）
- 内嵌标记违规修订对话框、内嵌源码只读化重构、标记精确落库与项目创建
  自愈（cac9f3a、483db1b）
- 服务退出统一清理子进程树：托盘退出后渲染沙盒/SDK translate 的 renpy.exe
  不再残留孤儿进程，卡在子进程等待里的任务随之立即返回；GUI 失联看门狗
  自动关窗（2aee366）

### 修复

- 台词解析：带点说话人（mc.name）、图像属性 say 与 monologue 三引号；
  tl 入库路径语言层与行尾参数（627ee82）
- AI 缓存命中的 usage 字段从 model_dump() 字典读取（0ced4f8）

## [v0.3.3] - 2026-08-25

### 修复

- 软件不携带中文字体（版权原因），此前安装版导出静默跳过字体步骤、中文无法显示：改为导出开始即预检——请将中文字体文件（.ttf/.otf/.ttc）放入 数据根/fonts/（数据根优先，可自行选择字体），未放置则终止导出并提示路径，不再产出中文不可显示的包（5f4aa17）

## [v0.3.2] - 2026-08-24

### 修复

- 导出游戏中文渲染为空白：字体映射此前只覆盖 Ren'Py 内置 DejaVuSans 家族，游戏自带字体（通常无 CJK 字形）完全漏网，且 gui.rpy 为反编译产物时字体补丁落空——现枚举 game/ 下全部自带字体，按相对路径+文件名两种写法进入 font_replacement_map（bcc4a39）
- 内嵌标记的译文导出后不生效：_() 包裹写在反编译文件里，导出时被删除，游戏跑原始 rpyc 永不查翻译表——现按"保留清单"导出含内嵌标记的反编译文件，编译校验报错时自愈按文件记入 dropped_decompiled_files 并重新导出回退原始 rpyc（bcc4a39）
- AI 精审 submit_verdicts 判决未覆盖整批（如缺 1 条）或参数损坏时整批抛错中断：改为受理部分判决、回喂缺失 id 追问补齐，轮数上限仍缺才失败（bcc4a39）

### 新增

- 导出游戏默认以中文启动：向已有 tl/chinese 骨架追加 init 块，persistent 标记保证仅首次强制，玩家之后在设置里切换的语言选择被保留（bcc4a39）

### 优化

- 语言切换按钮注入从单行精确匹配改为结构化定位：定位 screen preferences 块 + 语义锚点（null height pref_spacing / slider hbox），候选路径扩到 game/**/*.rpy（排除 tl/），耐模板改动；rpyc-only 游戏保留可注入的反编译 screens.rpy 作为按钮载体，找不到时响亮告警（bcc4a39）

## [v0.3.1] - 2026-08-18

### 修复

- 导出不再要求有已翻译内容：翻译不完全（甚至完全未翻译）也可导出，未译条目保持原文；任务日志给出分类计数提醒，导出页「开始导出」改为确认弹窗（2defeda）
- 上传 Release 步骤在 GitHub 服务故障（503/429/连接中断）时不再整步失败留下"已删未传"的资产缺口：改为 gh CLI 上传 + 服务端错误指数退避无限重试（真实错误仍立即失败）（c25bc40）

## [v0.3.0] - 2026-08-17

### 修复

- 构建流水线外部下载（unrpyc / python-embed / appimagetool）遇 codeload 429 限流时静默存下错误页、下游解压才报 BadZipFile：curl 改为 `-f --retry 5 --retry-all-errors`，限流自动重试、失败即报错（ebbddff）

### 新增

- 批次翻译新增内容级校验：疑似贴错行（译文与其他条目原文高度相似且明显高于本条原文，两条原文本身相似时不误报）、译文长度与原文悬殊的译文不入库，与未译出条目一起暂存待核验（975e7ec）
- 翻译页新增「失败条目」核验对话框：批量翻译结束后自动弹出，失败条目带原因逐条列出，支持全部重试（自选每批条数拆小批）、单句 AI 翻译、手动填写译文、清空暂存；直接关闭则保持未翻译，下次「全部翻译」重新拾起（975e7ec）

### 优化

- 批次翻译句数不匹配不再整批失败、不再中断任务：{id, translation} 按 id 落位，能匹配的译文照常入库，多次重试按 id 合并（同一句以最近一次返回为准），仅未译出/存疑条目暂存（975e7ec）

## [v0.2.2] - 2026-08-17

### 修复

- 安装版（Windows 打包）rpyc 反编译全失败：python-embed 的 ._pth 隔离模式下 sys.path 不含脚本目录，unrpyc 启动即崩溃（ModuleNotFoundError: decompiler），纯 rpyc 游戏无法创建项目。改为 -c 引导方式调用，手动将 unrpyc 目录插入 sys.path（42bff37）
- 脚本位于 game/scripts/ 等子目录的游戏误报"SDK 未生成翻译模板（只有 common.rpy）"：SDK 模板镜像脚本目录结构（tl/chinese/scripts/），存在性检查由非递归 glob 改为 rglob（42bff37）

## [v0.2.1] - 2026-08-16

### 新增

- 模型配置改为全局激活：在设置页激活一个配置，所有项目共用；新建首个配置自动激活（e02b7cb）
- 编辑模型配置时回显真实 API Key（新增 GET /configs/{name}/key 接口，列表仍为掩码）；测试连接可直接用已保存的 key（e02b7cb）

### 优化

- 项目不再单独选择模型：创建/编辑项目移除模型选择，项目「编辑」按钮改为「重命名」（旧项目的模型选择自动回退兼容）（e02b7cb）
- max_tokens 保存时校验必须为正整数，输入框注明它是单次回复输出上限而非上下文窗口（e02b7cb）

### 修复

- 保存模型配置后运行中的翻译器仍持旧 key/参数调用 LLM 导致调用失败：保存/激活后立即原地刷新翻译器，不打断进行中的批量任务（e02b7cb）

## [v0.2.0] - 2026-08-11

### 新增

- 游戏发布新版本后「更新版本」就地升级：已有译文按原文自动继承（微改句子模糊匹配预填 + 复核表待人工确认，失效旧译文保留可查），更新前自动备份（95db2ec）
- Ren'Py 7 引擎支持与 SDK 大版本自动检测（按 game/ 目录引擎版本选型）（733fbab）
- Ren'Py SDK 自动下载：先解压校验再替换旧目录，临时文件带 uuid 避免并发下载互相写坏（733fbab）
- 批量翻译句数不匹配（模型漏译/多译）自动重试，重试耗尽整批记失败（95db2ec）
- /api/health 返回版本号；新增 /api/health/deep 深度健康检查（验证 openai/tiktoken 等惰性加载依赖在打包环境可用）
- 术语表页面：AI 翻译自动积累的术语可视化管理，支持手动新增/删除/行内改译文、来源筛选、搜索与排序（058392c）
- 带截图的界面使用说明（docs/使用说明.md），README 已引用（058392c）
- 错误提示支持一键复制完整信息（058392c）

### 优化

- 人名翻译页：新增搜索与状态筛选（未翻译/已翻译/未分析），原文人名/台词数可排序（058392c）
- 字符串/对话翻译页：原文、角色列支持点击排序；行内编辑改为点击进入编辑态（058392c）
- 导出页改为标题行操作区 + 对话/字符串/人名分项进度条；全项目进度条统一为 8px 组件（058392c）

### 修复

- 版本更新失败/取消的回退问题：db 从备份恢复、game.bak 改回 game/，任何异常（含取消）都回滚到更新前状态（95db2ec）
- SDK 版本误选：版本探测/大版本选择基于项目根的 game/ 子目录而非项目根（35166d8）
- 迁移数据目录中途失败后应用库处于已关闭状态、后续接口全部 500：失败时重开旧应用库并尽量恢复原项目（35166d8）
- SPA 静态挂载目录穿越：resolve 后校验仍在 dist 内，防止 %2e%2e/ 读出任意文件（35166d8）
- SQLite 事务并发：实例级可重入锁串行化共享连接的一切读写（含事务），避免并发写撞游标/提前提交（35166d8）
- 取消盲区：筛查/精判/拆除、SDK 中止等下游取消统一转为 JobCancelled，任务状态正确落为 cancelled 而非 failed；建项/导入解压目录带 uuid 并登记清理，取消不留半成品（35166d8）
- 日志读取偶发 'deque mutated during iteration'：翻译线程写入与事件循环迭代竞争，抓 RuntimeError 重试取快照（35166d8）
- 任务事件流挂死：订阅前任务已终结时回放后必须关流，否则协程永久挂在 queue.get()（35166d8）
- characters.display_name 旧 UNIQUE 约束导致同名不同角色无法并存：建库时自动迁移拆约束（35166d8）

### 工程化

- 建立 tests/ 正式 pytest 体系（导出自愈、批次重试、迁移预检），pytest 配置 testpaths 避免误收集 tools/ 下的副作用脚本
- CI 冒烟加深：打包产物启动后额外请求 /api/health/deep 验证惰性加载依赖

## [v0.1.2] - 2026-08-08

### 新增

- 迁移数据目录存在被占用的残留文件时，自动重启服务完成清理：重启后自动删除旧目录残留，前端显示重启进度并自动刷新

### 修复

- 项目导出（项目包）任务无法取消：复制、打包各阶段加入取消检查点，取消时删除半成品 zip
- 游戏导出任务无法取消：复制游戏文件、填充翻译、编译校验自愈、打包各阶段均响应取消，取消时清理半成品
- 打包后打开项目崩溃（tiktoken 插件元数据缺失导致 cl100k_base 编码不可用）；编码器改为惰性加载，异常时降级为粗估，不再阻断项目打开
- 迁移数据目录时被占用的日志文件（gui.log 等）导致迁移中断：改为逐文件复制、占用文件跳过并记录残留清单

## [v0.1.1] - 2026-08-08

### 新增

- 导出自愈逻辑：导出结果不完整时自动检测并补齐
- 批量精判功能
- 任务取消提示
- 对话框找回

### 优化

- 项目导出与游戏导出体验
- 提取内嵌文本的可靠性
- 临时目录路径调整

### 修复

- 翻译进度显示问题
- 项目切换时页面未更新
- 页面切换时表格不更新
- 临时文件删除问题
- SDK 下载问题
- 退出服务后前端无法关闭

## [v0.1.0] - 首个公开版本

包含翻译、项目导入导出、反编译、字体与路径处理、arm 平台支持等基础功能。
